node_modules/
dist/
*.log
.env
.env.local
.DS_Store

# Forge models are large and must not be committed (volume-mounted at runtime).
forge/models/**
!forge/models/.gitkeep

# Local snapshots / exported scores
snapshots/
*.snapshot.json
